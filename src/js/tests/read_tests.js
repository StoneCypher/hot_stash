
/* eslint-disable max-len */

import { describe } from 'ava-spec';

const hot_stash = require('../../../build/hot_stash.es5.js');





describe('Test rig', async it => {

  it('can run tests', t => t.is(2, 2) );

});
